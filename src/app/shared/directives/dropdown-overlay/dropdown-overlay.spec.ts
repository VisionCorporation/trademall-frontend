import { DropdownOverlay } from './dropdown-overlay';

describe('DropdownOverlay', () => {
  it('should create an instance', () => {
    const directive = new DropdownOverlay();
    expect(directive).toBeTruthy();
  });
});
